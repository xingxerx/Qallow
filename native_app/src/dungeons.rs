//! Placeholder for the "dungeons" exploratory subsystem referenced by the
//! project's module layout. No behavior is wired up to the UI yet.

#[derive(Debug, Clone, Default)]
pub struct DungeonState {
    pub depth: u32,
}
