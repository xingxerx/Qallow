# Core AGI Framework
qdrant-client>=1.11.0
sentence-transformers>=2.7.0
arxiv>=2.1.0
httpx>=0.27.0

# Agent Orchestration
langgraph>=0.2.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Tracing & Observability
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp>=1.25.0

# Utilities
asyncio
dataclasses
uuid

# Kimi-K2 Integration (Local Inference)
openai>=1.0.0              # OpenAI-compatible API for local inference
vllm>=0.10.0               # vLLM inference engine (recommended)
sglang>=0.1.0              # SGLang inference engine (alternative)
transformers>=4.30.0       # For tokenizers and model loading
peft>=0.4.0                # Parameter-efficient fine-tuning (optional)

# Chat Server
fastapi>=0.104.0
uvicorn>=0.24.0
pyyaml>=6.0
