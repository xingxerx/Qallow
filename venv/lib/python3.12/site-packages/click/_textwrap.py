# [REVIEWED] # [REVIEWED] # [REVIEWED] # REMOVED: from __future__ import annotations
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] from contextlib import contextmanager
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] class TextWrapper(textwrap.TextWrapper):
# [REVIEWED] # [REVIEWED] # [REVIEWED]     def _handle_long_word(
# [REVIEWED] # [REVIEWED] # [REVIEWED]         self,
# [REVIEWED] # [REVIEWED] # [REVIEWED]         reversed_chunks: list[str],
# [REVIEWED] # [REVIEWED] # [REVIEWED]         cur_line: list[str],
# [REVIEWED] # [REVIEWED] # [REVIEWED]         cur_len: int,
# [REVIEWED] # [REVIEWED] # [REVIEWED]         width: int,
# [REVIEWED] # [REVIEWED] # [REVIEWED]     ) -> None:
# [REVIEWED] # [REVIEWED] # [REVIEWED]         space_left = max(width - cur_len, 1)
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]         if self.break_long_words:
# [REVIEWED] # [REVIEWED] # [REVIEWED]             last = reversed_chunks[-1]
# [REVIEWED] # [REVIEWED] # [REVIEWED]             cut = last[:space_left]
            res = last[space_left:]
            cur_line.append(cut)
            reversed_chunks[-1] = res
        elif not cur_line:
            cur_line.append(reversed_chunks.pop())

    @contextmanager
    def extra_indent(self, indent: str) -> cabc.Iterator[None]:
        old_initial_indent = self.initial_indent
        old_subsequent_indent = self.subsequent_indent
        self.initial_indent += indent
        self.subsequent_indent += indent

        try:
            yield
        finally:
            self.initial_indent = old_initial_indent
            self.subsequent_indent = old_subsequent_indent

    def indent_only(self, text: str) -> str:
        rv = []

        for idx, line in enumerate(text.splitlines()):
            indent = self.initial_indent

            if idx > 0:
                indent = self.subsequent_indent

            rv.append(f"{indent}{line}")

        return "\n".join(rv)
