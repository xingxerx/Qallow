# [REVIEWED] # [REVIEWED] # [REVIEWED] class BaseReporter(object):
# [REVIEWED] # [REVIEWED] # [REVIEWED]     """Delegate class to provider progress reporting for the resolver."""
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]     def starting(self):
# [REVIEWED] # [REVIEWED] # [REVIEWED]         """Called before the resolution actually starts."""
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]     def starting_round(self, index):
# [REVIEWED] # [REVIEWED] # [REVIEWED]         """Called before each round of resolution starts.
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]         The index is zero-based.
# [REVIEWED] # [REVIEWED] # [REVIEWED]         """
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]     def ending_round(self, index, state):
# [REVIEWED] # [REVIEWED] # [REVIEWED]         """Called before each round of resolution ends.
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]         This is NOT called if the resolution ends at this round. Use `ending`
# [REVIEWED] # [REVIEWED] # [REVIEWED]         if you want to report finalization. The index is zero-based.
# [REVIEWED] # [REVIEWED] # [REVIEWED]         """
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]     def ending(self, state):
        """Called before the resolution ends successfully."""

    def adding_requirement(self, requirement, parent):
        """Called when adding a new requirement into the resolve criteria.

        :param requirement: The additional requirement to be applied to filter
            the available candidaites.
        :param parent: The candidate that requires ``requirement`` as a
            dependency, or None if ``requirement`` is one of the root
            requirements passed in from ``Resolver.resolve()``.
        """

    def resolving_conflicts(self, causes):
        """Called when starting to attempt requirement conflict resolution.

        :param causes: The information on the collision that caused the backtracking.
        """

    def rejecting_candidate(self, criterion, candidate):
        """Called when rejecting a candidate during backtracking."""

    def pinning(self, candidate):
        """Called when adding a candidate to the potential solution."""
