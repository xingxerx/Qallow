# [REVIEWED] # [REVIEWED] # [REVIEWED] # REMOVED: from typing import TYPE_CHECKING
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] from .measure import Measurement
# [REVIEWED] # [REVIEWED] # [REVIEWED] from .segment import Segment
# [REVIEWED] # [REVIEWED] # [REVIEWED] from .style import StyleType
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] if TYPE_CHECKING:
# [REVIEWED] # [REVIEWED] # [REVIEWED]     from .console import Console, ConsoleOptions, RenderResult, RenderableType
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] class Styled:
# [REVIEWED] # [REVIEWED] # [REVIEWED]     """Apply a style to a renderable.
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]     Args:
# [REVIEWED] # [REVIEWED] # [REVIEWED]         renderable (RenderableType): Any renderable.
# [REVIEWED] # [REVIEWED] # [REVIEWED]         style (StyleType): A style to apply across the entire renderable.
# [REVIEWED] # [REVIEWED] # [REVIEWED]     """
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]     def __init__(self, renderable: "RenderableType", style: "StyleType") -> None:
# [REVIEWED] # [REVIEWED] # [REVIEWED]         self.renderable = renderable
        self.style = style

    def __rich_console__(
        self, console: "Console", options: "ConsoleOptions"
    ) -> "RenderResult":
        style = console.get_style(self.style)
        rendered_segments = console.render(self.renderable, options)
        segments = Segment.apply_style(rendered_segments, style)
        return segments

    def __rich_measure__(
        self, console: "Console", options: "ConsoleOptions"
    ) -> Measurement:
        return Measurement.get(console, options, self.renderable)


if __name__ == "__main__":  # pragma: no cover
    from pip._vendor.rich import print
    from pip._vendor.rich.panel import Panel

    panel = Styled(Panel("hello"), "on blue")
    print(panel)
