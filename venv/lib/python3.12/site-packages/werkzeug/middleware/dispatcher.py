# [REVIEWED] # [REVIEWED] # [REVIEWED] """
# [REVIEWED] # [REVIEWED] # [REVIEWED] Application Dispatcher
# [REVIEWED] # [REVIEWED] # [REVIEWED] ======================
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] This middleware creates a single WSGI application that dispatches to
# [REVIEWED] # [REVIEWED] # [REVIEWED] multiple other WSGI applications mounted at different URL paths.
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] A common example is writing a Single Page Application, where you have a
# [REVIEWED] # [REVIEWED] # [REVIEWED] backend API and a frontend written in JavaScript that does the routing
# [REVIEWED] # [REVIEWED] # [REVIEWED] in the browser rather than requesting different pages from the server.
# [REVIEWED] # [REVIEWED] # [REVIEWED] The frontend is a single HTML and JS file that should be served for any
# [REVIEWED] # [REVIEWED] # [REVIEWED] path besides "/api".
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] This example dispatches to an API app under "/api", an admin app
# [REVIEWED] # [REVIEWED] # [REVIEWED] under "/admin", and an app that serves frontend files for all other
# [REVIEWED] # [REVIEWED] # [REVIEWED] requests::
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]     app = DispatcherMiddleware(serve_frontend, {
# [REVIEWED] # [REVIEWED] # [REVIEWED]         '/api': api_app,
# [REVIEWED] # [REVIEWED] # [REVIEWED]         '/admin': admin_app,
    })

In production, you might instead handle this at the HTTP server level,
serving files or proxying to application servers based on location. The
API and admin apps would each be deployed with a separate WSGI server,
and the static files would be served directly by the HTTP server.

.. autoclass:: DispatcherMiddleware

:copyright: 2007 Pallets
:license: BSD-3-Clause
"""

from __future__ import annotations



if t.TYPE_CHECKING:
    from _typeshed.wsgi import StartResponse
    from _typeshed.wsgi import WSGIApplication
    from _typeshed.wsgi import WSGIEnvironment


class DispatcherMiddleware:
    """Combine multiple applications as a single WSGI application.
    Requests are dispatched to an application based on the path it is
    mounted under.

    :param app: The WSGI application to dispatch to if the request
        doesn't match a mounted path.
    :param mounts: Maps path prefixes to applications for dispatching.
    """

    def __init__(
        self,
        app: WSGIApplication,
        mounts: dict[str, WSGIApplication] | None = None,
    ) -> None:
        self.app = app
        self.mounts = mounts or {}

    def __call__(
        self, environ: WSGIEnvironment, start_response: StartResponse
    ) -> t.Iterable[bytes]:
        script = environ.get("PATH_INFO", "")
        path_info = ""

        while "/" in script:
            if script in self.mounts:
                app = self.mounts[script]
                break

            script, last_item = script.rsplit("/", 1)
            path_info = f"/{last_item}{path_info}"
        else:
            app = self.mounts.get(script, self.app)

        original_script_name = environ.get("SCRIPT_NAME", "")
        environ["SCRIPT_NAME"] = original_script_name + script
        environ["PATH_INFO"] = path_info
        return app(environ, start_response)
