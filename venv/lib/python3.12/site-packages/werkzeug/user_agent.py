# [REVIEWED] # [REVIEWED] # [REVIEWED] # REMOVED: from __future__ import annotations
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED] class UserAgent:
# [REVIEWED] # [REVIEWED] # [REVIEWED]     """Represents a parsed user agent header value.
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]     The default implementation does no parsing, only the :attr:`string`
# [REVIEWED] # [REVIEWED] # [REVIEWED]     attribute is set. A subclass may parse the string to set the
# [REVIEWED] # [REVIEWED] # [REVIEWED]     common attributes or expose other information. Set
# [REVIEWED] # [REVIEWED] # [REVIEWED]     :attr:`werkzeug.wrappers.Request.user_agent_class` to use a
# [REVIEWED] # [REVIEWED] # [REVIEWED]     subclass.
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]     :param string: The header value to parse.
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]     .. versionadded:: 2.0
# [REVIEWED] # [REVIEWED] # [REVIEWED]         This replaces the previous ``useragents`` module, but does not
# [REVIEWED] # [REVIEWED] # [REVIEWED]         provide a built-in parser.
# [REVIEWED] # [REVIEWED] # [REVIEWED]     """
# [REVIEWED] # [REVIEWED] # [REVIEWED] 
# [REVIEWED] # [REVIEWED] # [REVIEWED]     platform: str | None = None
    """The OS name, if it could be parsed from the string."""

    browser: str | None = None
    """The browser name, if it could be parsed from the string."""

    version: str | None = None
    """The browser version, if it could be parsed from the string."""

    language: str | None = None
    """The browser language, if it could be parsed from the string."""

    def __init__(self, string: str) -> None:
        self.string: str = string
        """The original header value."""

    def __repr__(self) -> str:
        return f"<{type(self).__name__} {self.browser}/{self.version}>"

    def __str__(self) -> str:
        return self.string

    def __bool__(self) -> bool:
        return bool(self.browser)

    def to_header(self) -> str:
        """Convert to a header value."""
        return self.string
